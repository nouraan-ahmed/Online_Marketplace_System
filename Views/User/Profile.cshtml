@model IEnumerable<Online_MarketPlace_System.Models.Product>

        <div class="container">
            <h1>Profile User</h1>

            @{
                ViewData["Title"] = "Part";
            }

            <div class="container p-3">
                <div class="row pt-4">
                    <div class="col-6">
                        <h2 class="text-primary">Products List</h2>
                    </div>
                    <div class="col-6 text-right">
                        <a asp-action="AddProduct" class="btn btn-primary">
                            <i class="fas fa-plus"></i> &nbsp; Add New Product
                        </a>
                    </div>
                </div>

                <br /><br />

                @if (Model.Count() > 0)
                {
                    <table class="table table-bordered table-striped" style="width:100%">
                        <thead>
                            <tr>
                                <th>
                                    Name
                                </th>

                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var obj in Model)
                            {
                                <tr>
                                    <td width="50%">@obj.Name</td>
                                    <td class="text-center">
                                        <div class="w-75 btn-group" role="group">
                                            <a asp-route-Id="@obj.Name"  class="btn btn-primary mx-2">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <a asp-route-Id="@obj.Price"  class="btn btn-danger mx-2">
                                                <i class="far fa-trash-alt"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
                else
                {
                    <p> No Products you have yet.</p>
                }

            </div>

            <div class="form-group">
               
                <div>
                    <a class="btn btn-primary mx-2" href="/Home/Index">Back to the Home Page</a>
                </div>

            </div>
        </div>

